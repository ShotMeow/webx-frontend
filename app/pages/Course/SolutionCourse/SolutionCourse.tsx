import React, {FC} from 'react';
import {ICourse} from "../../../types/api.types";

const SolutionCourse: FC<{course: ICourse; setStep: (step: number) => void}> = ({course, setStep}) => {
    return (
        <div>

        </div>
    );
};

export default SolutionCourse;