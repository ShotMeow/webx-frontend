import React, {FC} from 'react';
import {ICourse} from "../../../types/api.types";

const ResultCourse: FC<{course: ICourse; setStep: (step: number) => void}> = ({course, setStep}) => {
    return (
        <div>

        </div>
    );
};

export default ResultCourse;